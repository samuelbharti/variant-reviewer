*.Rproj
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Local config/secrets
.Renviron

# renv local library/cache (keep renv.lock tracked)
renv/library/
renv/staging/

# Local development helpers
dev/.Rhistory

# Local agent/plan/scratch artifacts (keep docs/plans/ tracked)
.claude/
/plans/
/skills/
scratchpad/
docs/local/

# Local caches and any downloaded reference data. Small bundled files (e.g. the
# MANE summary) are tracked deliberately; anything landing here is not.
data/cache/
*.vcf.gz
*.vcf.gz.tbi
